[pytest]
markers =
    unit: fine-grained tests that do not require external services
    integration: tests exercising multiple components or external dependencies
    e2e: end-to-end tests covering full system flows
